export enum NameSpaces {
  BLOGS = '/blogs',
  UNIQ_GUEST = '/uniq-guest',
  ADMIN = '/admin'
}

export enum EmitEvents {
  new_comment = 'new_comment',
  welcome = 'welcome',
  upload_done = 'upload_done'
}
